/**
 * BatchQueue — placeholder for v2 batch resize feature.
 * Will support multiple image uploads, resize all at once,
 * and download as a zip archive.
 */
export default function BatchQueue() {
  return (
    <div className="rounded-xl border border-dashed border-gray-700 p-6 text-center text-gray-500">
      <p className="text-sm">Batch resize coming in v2</p>
    </div>
  );
}
